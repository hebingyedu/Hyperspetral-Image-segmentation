必须以#开头，否则该行省略，省略掉空格
类型（暂时只支持python,如果使用的是c的话，可以编译成库用python封装一下)
#methodtype: python
主文件
# mainfile:showRGB.py
主函数
# mainmethod: showRGB
图标
#icon:icon.png
类型
#toolclass:custom
输入 类型，参数定义，名称，默认值(可以没有)
# input: type="bands",name="img_r",show="R"
# input: type="bands",name="img_g",show="G"
# input: type="bands",name="img_b",show="B"
#input: type="num",name="a",show="sigma",default="0.1"

输出 上面的换成output.只支持图像输出，以及参数输出，不需要default参数。
# output: type="image", name="outim", show="输出"

